# invoices.ainsof.io — מצב העבודה (עודכן 1.9.2026, סשן שני)

## הסטטוס בשורה אחת
העיצוב מוכן ונשלח לאור לאישור. **טרם אושר.** לא בוצע commit, לא עלה לאוויר.

## מה השתנה בסשן הזה
1. **התמונות עובדות שוב.** בסשן הקודם כל תמונה נדחתה ע"י ה-API ועבדתי עיוור.
   הפעם צילמתי, ראיתי, ושלחתי לאור שלושה קבצים.
2. **נמצא ותוקן פגם אמיתי שהעיוורון הסתיר** — ראה למטה.

## הפגם שתוקן: פס אופקי ברקע הקולאז'
`<img class="cbg" src="section-bg-tools-coral.jpg">` — לאלמנט היה גם `src` וגם
`background:...repeat-y` ב-CSS. כלומר הוא צייר את הלוח **פעמיים**: פעם מרוצף
(הכוונה), ופעם אחת בגודל טבעי (`object-fit:none`) ממורכז בשדה — עותק יחיד בגובה
1018px עם קצה חד למעלה ולמטה, מעל הריצוף.
נמדד: קפיצות בהירות ב-y=420 ו-y=1438 — בדיוק (1731-1018)/2 מעל ומתחת למרכז.
**התיקון:** `<div class="cbg">` במקום `<img>`. הריצוף הוא ה-CSS background בלבד;
האלמנט לא נושא תמונה משלו. אחרי התיקון: 0 קפיצות לאורך כל השדה.

הערת ה-HANDOFF הקודם "אין תפר כי ארבע הפינות הן `--bg`" הייתה נכונה לגבי הריצוף
אבל לא לקחה בחשבון את ה-`<img>` — ולכן פספסה את הפס.

## אימות אחרון (Playwright, Chromium + WebKit, 1440 ו-390)
- 1440: 18 קלפים, `overflowX:false`, 0 קלפים חורגים, 0 תפרים ברקע.
- 390: 8 קלפים, `overflowX:false`, ניווט **נכנס** (navRight 331 ≤ 390, הכותרת
  נשברת לשתי שורות, topH=90). שני קלפים (k0, k9) גולשים 23px ו-8px שמאלה
  ונחתכים ב-`overflow:hidden` — bleed מכוון בסגנון ארטליסט, לא באג.
- WebKit זהה לכרומיום בשני הרוחבים (אור גולש בספארי).

## ⚠️ מלכודת כלים — לא לחזור עליה
`Google Chrome --headless=new --window-size=390,...` **לא** מגדיר את ה-layout
viewport ל-390. הוא מצייר ב-~800px וחותך ל-390, אז נראה כאילו הניווט חתוך
במובייל. צילומי 429 ו-431 יצאו זהים לחלוטין = ה-media query של ≤430 לא נדלק =
הוכחה שהרוחב הלוגי אינו רוחב החלון. **למובייל: Python Playwright**
(`/opt/homebrew/lib/python3.14/site-packages/playwright`, יש chromium-1223
ו-webkit-2287 מותקנים). `agent-browser` לא יודע לשנות viewport — `resize`
ו-`--viewport` לא משנים את `innerWidth`.
מלכודת שנייה: `--full` screenshot תופס לפני ש-`loading="lazy"` צייר. לפני צילום
מלא — לגלול, `i.loading='eager'`, ואז `Promise.all(imgs.map(i=>i.decode()))`.

## מה אור ביקש, לפי הסדר, ומה הוא פסל
1. "באותה שפה ועיצוב כמו ainsof.io, פונקציונלית אל תיגע" → v1 → **"לא טוב"**
2. "באנר שזז במושן, תיקח מכריכות הפלייליסטים של יוטיוב"
3. הצביע על artlistmusic.io → העתקתי את **המותג** שלהם (מונוכרום #171717,
   כפתורי גלולה, בלי סריף) → **"גרוע, זה לא העיצוב שלנו והשפה שלנו"**
4. "תראה את הרקע, כבר יש לנו רקע כזה בדף הראשי" → לוחות מצוירים, לא גרדיאנט
5. "תראה אצלם איזה גדול זה החלק הזה" → הסקשן, לא הקלפים

## הכלל המרכזי
**את הקומפוזיציה לוקחים מארטליסט. את השפה — מ-ainsof.io. אף פעם לא הפוך.**

## מה שנמדד חי מ-artlistmusic.io (1440 רוחב)
15 קלפים, שדה 3038px (338% ממסך), הקלף הגדול 315px = 22% מהרוחב, כל המסגרות
`border-radius: 0`. **התנועה היא פרלקסה בגלילה בלבד** — במנוחה אפס תזוזה
(דגימה כל 250ms במשך 3.5s). קצב שונה לכל קלף ובשני הצירים.

## מצב הקוד
- 18 קלפים, שדה 300% ממסך, קלף גדול 23% מהרוחב
- רקע: `section-bg-tools-coral.jpg` מרוצף אנכית (repeat-y, 118%) +
  `grain-copper.png` ב-overlay
- טיפוגרפיה/צבע/כפתורים = ainsof.io (Newsreader 500, `--coral`, radius 0)
- **הטופס והסקריפט שלו לא נגעתי בהם** — diff מול הגרסה שלפני התיקון: שינוי אחד
  בלבד, שורה 588 (ה-`<img class="cbg">`)
- `git status`: `M index.html` + נכסים חדשים ללא מעקב. אין commit.

## מה עוד פתוח
1. **אור עדיין לא אישר את העיצוב** — זה החסם היחיד.
2. אחרי אישור: commit + push (הריפו `AINSOF-Tech/submit-invoice`, ענף `main`,
   GitHub Pages עם CNAME ל-invoices.ainsof.io). **לא לדחוף בלי אישור מפורש.**
