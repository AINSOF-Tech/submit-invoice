# invoices.ainsof.io — מצב העבודה (עודכן 2.9.2026)

## הסטטוס בשורה אחת
**באוויר.** commit `5026cac` על `main` של `AINSOF-Tech/submit-invoice`,
GitHub Pages, CNAME ל-invoices.ainsof.io. אושר ע"י אור ונדחף.

## מה הדף
טופס הגשת חשבונית למלחינים ולשותפים. ארבעה שלבים, שליחה ל-Google Apps Script.
**הטופס והסקריפט שלו לא נגעו בהם לאורך כל העיצוב מחדש** — מלבד שלושה
`placeholder` שהשם בהם השתנה ל-John Bille.

## החוק המרכזי של העיצוב
**את הקומפוזיציה לוקחים מ-artlistmusic.io, את השפה מ-ainsof.io. אף פעם לא הפוך.**
כל מספר בקומפוזיציה נמדד חי ב-DevTools ב-1440, לא הועתק מצילום מסך:
סקשן 1440×603 בריפוד `64px 99.03px 0`, כותרת 50.49px, ארבעה קלפים 287×290
ב-`border-radius:40px` עם מרווח 25px, והשורה 86.25% מהחלון — **אחוז, לא px קבוע**.
נעילה על 1242px גרמה לקלפים להתכווץ בכל מסך רחב מ-1440.

## כפתורים — חוק קבוע של אור
כל כפתור בדף נראה כמו `.btn` (זכוכית + blur + טבעת קורל ב-hover).
וריאנט מותר רק בגודל, אף פעם לא בטיפול.
מתועד ב-`feedback_all_buttons_match_the_primary_button.md`.

## קריאוּת — איך בודקים, ולמה בדיקה רגילה משקרת
הרקע כאן הוא **לוח מצויר**, לא צבע. בדיקת ניגודיות שמרכיבה צבע-על-צבע עוברת
ומפספסת הכל. הדרך הנכונה: לצלם את הדף פעמיים, פעם אחת עם
`* { color: transparent }`, ואז הרקע האמיתי מתחת לכל שורת טקסט ידוע.
כך נמצאו 3.28-3.89 על הדף החי אחרי שבדיקה רגילה אמרה "נקי".
**מלכודת שנייה:** הטופס וסרגל ההתקדמות מוסתרים עד שלוחצים Start. בדיקה שלא
לוחצת Start מודדת שליש מהדף. הלחיצה חשפה מיד עוד ממצא (`Step 1 of 4` ב-2.12).
הסקריפט בריפו: `tools/contrast-check.py <url> <label>`.

## טוקנים שנוספו ולמה
- `--coral-ink:#E2AE91` — `--coral` הוא צבע משטח; כגליף טקסט הוא מודד מתחת ל-3.
  כל גליף קוראלי בדף משתמש בזה: החצים, סימן ה-V, שם הקובץ שהועלה.
- `--req:#E89393` — סימן החובה חייב להישאר אדום כדי להיקרא כחובה.
- `--ink-3` חזר להיות **דקורציה בלבד**. כל טקסט שצריך לקרוא הוא `--ink-2`.

## נכסים
`section-bg-tools-teal.jpg` (3400×1018) הוא הלוח שמאחורי ההנחיות — כתום עם
טורקיז מעורבב, נבנה offline ב-Lanczos כדי לא להגדיל רעש JPEG בדפדפן.
**ארבע הפינות של כל לוח הן `rgb(16,16,18)` = `--bg`** — לכן הם נמסים בדף
במקום להיגמר על קצה. לבדוק את זה אחרי כל ייצוא מחדש.
`glow-bg.png`, `top.png`, `section-bg-tools-wide.jpg` — לא מקושרים, ב-`.gitignore`.

## מה נבדק לפני הפריסה
32 תצורות (16 רוחבים 320-2560 × Chromium ו-WebKit, כל השלבים פתוחים):
אפס גלילה אופקית, אפס חריגה מהמסך, אפס גלישת טקסט, אפס תמונה שבורה,
אפס שגיאת JS, 44px לכל יעד מגע בטלפון, אפס טקסט מתחת ל-12px.
108 שורות טקסט עוברות 4.5:1 מול הרקע האמיתי (הכלי מחזיר exit 1 אם אחת נופלת).

## מה לא נבדק, במכוון
שליחת טופס אמיתית — ה-endpoint חי ובדיקה הייתה יוצרת רשומה מזויפת.
